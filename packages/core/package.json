{
  "name": "@awake-os/core",
  "version": "0.0.0",
  "private": true,
  "description": "Pure-TypeScript core: 8 frozen data contracts + the 8-stage EVENT -> REFLECTION pipeline. Platform-agnostic, zero runtime dependencies.",
  "type": "module",
  "main": "./src/index.ts",
  "react-native": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "node --test \"tests/**/*.test.ts\"",
    "typecheck": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2"
  }
}
